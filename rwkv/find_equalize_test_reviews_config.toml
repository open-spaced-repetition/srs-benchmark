DATA_PATH = "../anki-revlogs-10k"
LABEL_FILTER_LMDB_PATH = "label_filter_db"
LABEL_FILTER_LMDB_SIZE = 7_000_000_000

# Feature-engineering settings used to decide which reviews are part of the benchmark.
# These mirror CLI flags in `config.py` and control how `create_features()` filters/splits data.
ALGO = "FSRS-6"
SHORT = true
SECS = true

USER_START = 1
USER_END = 10000

PROCESSES = 9