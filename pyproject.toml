[project]
name = "fsrs-optimizer-project"
version = "0.1.0"
dependencies = [
    "pandas",
    "matplotlib",
    "scikit-learn",
    "fsrs-optimizer",
    "huggingface_hub",
    "wandb",
    "torch",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
backend-path = "."
